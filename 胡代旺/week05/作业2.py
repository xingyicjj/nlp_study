from openai import OpenAI

# 初始化客户端，指向 Ollama 的本地服务
client = OpenAI(
    base_url="http://localhost:8001/v1",  # Ollama API 地址
    api_key="aaa"  # Ollama 默认无需真实 API Key，填任意值即可,必须填写
)

# 发送请求
response = client.chat.completions.create(
    model="qwen3:0.6b",  # 指定模型
    messages=[
        {"role": "system", "content": "你是一个有帮助的助手。"},
        {"role": "user", "content": "你好，什么是大模型？"}
    ],
    temperature=0.7,  # 控制生成多样性
    max_tokens=512    # 最大生成 token 数
)

# 打印结果
print(response.choices[0].message.content)


# <think>
# 好的，用户问“什么是大模型？”，我需要先解释清楚。首先，大模型是指由大型语言模型组成的系统，这些模型通过海量数据训练而成，具备理解和生成复杂语言的能力。用户可能对大模型的概念不太了解，需要先明确其组成和功能。
#
# 接下来，我应该分点说明，比如定义、组成、训练数据、应用场景等。可能还需要举一些例子，比如通用大模型如GPT，或者特定领域如医疗、金融等应用。同时，要强调其优势，比如强大的语言处理能力、跨领域应用等，让用户有一个全面的认识。
#
# 还要注意用户可能的误区，比如大模型是否只是简单的AI，或者是否有训练数据。需要澄清这一点，说明训练数据的重要性。此外，可能需要提到隐私和安全的问题，但用户的问题比较基础，不需要深入。最后，确保回答简洁明了，结构清晰，便于用户理解和记忆。
# </think>
#
# 大模型是指由大量语言训练数据经过海量计算训练的大型人工智能系统。它能够理解和生成复杂语言，具备强大的语言处理能力，并在多个领域（如自然语言、数学、编程等）中表现出色。以下是对大模型的详细解释：
#
# 1. **定义**：
# 大模型是基于大规模语言训练数据（如互联网文本、学术论文等）经过深度学习训练的AI系统，能够完成文本生成、理解语言、推理、对话等多种任务。
#
# 2. **组成**：
# - **训练数据**：包含海量文本，涵盖各种领域，如科学、技术、文学、日常对话等。
# - **算法**：使用深度神经网络等先进算法，通过大量训练数据优化模型参数。
# - **训练过程**：通过大量数据训练模型，使其学习语言规律，从而提升性能。
#
# 3. **应用场景**：
# - **语言处理**：如智能助手、翻译工具、文本生成等。
# - **跨领域应用**：如医疗诊断、金融分析、法律咨询等。
# - **多语言能力**：支持多种语言的交互与理解。
#
# 4. **优势**：
# - **强大的语言处理能力**：能够理解和生成复杂语言，处理自然语言的语法、逻辑等。
# - **广泛的应用场景**：适用于需要多语言或复杂任务的领域，如客服、教育、科研等。
# - **适应性强**：无需专业知识即可使用，提升效率。